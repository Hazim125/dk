## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting in Arabic

## Notes
The application requires an RTL layout direction.
Tailwind config should support RTL variants if not already enabled (default in v3+).
Authentication uses cookie-based sessions.
Images for avatars should use placeholders if not provided.
